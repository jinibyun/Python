# sys.path: to call module programmer created.

import sys
sys.path # return the location where all python modules are stored. These modules can be called anywhere.

# NOTE: how to append other folder to sys.path
# sys.path.append("C:/blah/blah") NOTE: "/" or "\\" only can be used (Not "\")